services:
  - type: web
    name: Deployable-Express-React-Scaffold
    runtime: node
    plan: free
    buildCommand: npm install && npm run build
    startCommand: npm start
    envVars:
      - key: NODE_VERSION
        value: 20
